/** @type {import('next').NextConfig} */
const nextConfig = {
  /* konfigurasi kamu lainnya di sini (kosongkan dulu gak apa-apa biar aman) */
};

export default nextConfig; // 🌟 GANTI BARIS INI (Gaya ES Module)